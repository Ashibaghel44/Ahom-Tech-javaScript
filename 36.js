// Create a function that converts a string to an array of characters


function array(str) {
    return str.split('');
  }
  
const inputString = "i love travelling";
const con  = array(inputString);
console.log('Array of characters:', con ); 